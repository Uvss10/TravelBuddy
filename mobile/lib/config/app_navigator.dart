import 'package:flutter/material.dart';

/// App-wide navigator handle for UI elements rendered above Navigator context.
class AppNavigator {
  static final GlobalKey<NavigatorState> key = GlobalKey<NavigatorState>();
}
